.venv/
.env
data/*.log
__pycache__/
*.pyc

# The database is accumulated state, kept in the GitHub Actions cache rather
# than in git — committing a binary file eight times a day raced every
# developer push and conflicted constantly.
data/*.db
data/*.log
data/digest_preview.*
